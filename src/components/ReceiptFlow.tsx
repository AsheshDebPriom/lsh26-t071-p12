"use client";

/** Placeholder until phase 5 wires up the reader and the review screen. */
export type ReceiptFlowProps = {
  today: string;
  knownCategories: string[];
  onSaved: () => void;
  onBack: () => void;
};

export function ReceiptFlow(props: ReceiptFlowProps) {
  void props;
  return null;
}
